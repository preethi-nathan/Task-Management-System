$kind: http-request
name: 'Delete'
method: DELETE
url: '{{baseUrl}}/api/tasks/1'
order: 4000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status code is 204 or 200', () => pm.expect(pm.response.code).to.be.oneOf([200,204]));
